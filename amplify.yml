version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd my-app
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: my-app/.next
        files:
          - '**/*'
      cache:
        paths:
          - my-app/node_modules/**/*
